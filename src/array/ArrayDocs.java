package array;

public class ArrayDocs {
/*
 * -------------Array-----------
 * ->variable can only store only one value at a time.
 * ->variable take a least value or current value.
 * 
 *  Array should be fixed size, same data type, 
 *  
 *  # use to store and processing static multiple data.(fixed size)
 *  # collection of homogeneous data.(same data type).
 *  # collection / group of multiple values of same data type with fixed size.
 *  # data stored on the basis of indices (index start from 0).
 *  # also contain duplicate data.
 *  # contiguous memory allocation.
 *  # array is an object in java.
 *  
 *  # Types:
 *  a> Single-D Array :
 *     syntax:
 *     
 *     data_type array_name[] = new data_type[size];
 *  
 *  b> Multi-D Array
 *  syntax:
 *   
 *     data_type array_name[][] = new data_type[row][col];
 *     
 *     e.g.
 *        int price[] = new int[50];
 *        float height = new float[500];
 *        char []vs = new char[5];
 *        
 *        
 *        Employee[] emps = new Employee[500];
 *  
 * */
}
